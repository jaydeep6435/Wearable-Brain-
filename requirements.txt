# Wearable Brain - Offline Conversational Memory Assistant

# Core speech pipeline (offline)
faster-whisper>=1.0.0
pyannote.audio==3.1.1

# pyannote 3.1.x compatibility pins (Python 3.11)
torch==2.10.0
torchaudio==2.10.0
numpy<2
huggingface_hub==0.24.6

# Audio capture / processing
sounddevice
pydub

# NLP and event parsing
dateparser

# API and local integration
flask
flask-cors
requests

# Security and semantic memory utilities
cryptography>=41.0
# Keep transformers stack compatible with huggingface_hub 0.24.x
sentence-transformers<5

# Testing
pytest
